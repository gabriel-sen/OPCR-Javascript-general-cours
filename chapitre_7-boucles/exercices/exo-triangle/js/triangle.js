// version for faire une tyramise dans le console log
var symbol = "";
for (var i = 1; i <= 5; i++) {
  symbol = symbol + "#";
  console.log (symbol)
}

// version while faire une tyramise dans le console log
var symbole = ""
while (symbole != "#####") {
  symbole = (symbole+"#");
  console.log(symbole)
}
